version: '3.4'

services:
  spicecraft-client:
    build:
      context: .
      dockerfile: spicecraft.client/Dockerfile
    image: spicecraft-client:latest
    ports:
      - "8082:80"
    

  spicecraft-server:
    build:
      context: .
      dockerfile: SpiceCraft.Server/Dockerfile
    image: spicecraft-server:latest
    ports:
      - "8080:80"

    
